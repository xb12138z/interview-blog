# 回调路由

核心负责在http模块整体使用完毕后，获得了对应的操作要求和消息。handlers负责根据操作路径进行触发，对消息进行处理。

## ChatSendHandler

负责消息的回复处理。handler函数中传入req和resp两个参数。通过解析req参数获得对话者的session。从中解析出来对话历史记录和用户登录状态信息。确认永辉登录后从req中解析出用户的问题、调用模型的种类，然后创建**AIHelper**实例，负责调用ai回答用户。最后将回答封装为resp中输出。
