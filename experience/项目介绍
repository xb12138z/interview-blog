# 项目介绍

## ai项目介绍：

我这个ai项目主要实现了一个基于rag的产品售后助手，主要负责解决一些售后方面的问题。项目主要实现了以下几种功能： 

首先网络的注册登录功能，此处基于TCP实现，保证注册登录信息的完整性和可靠性。注册时会将密码存入数据库中，登录时匹配用户名和密码，进行登录检测。  

登录后项目界面会有显示历史对话的功能，每个历史对话会有对应的uuid（由当前时间戳+随机数生成），存储在cookie中。在对话时，为了解决并发问题，会通过RabbitMQ将消息写入到数据库中。当用户登录成功后，服务器会从数据库中拿出对应用户的消息放在内存中，以便快速获取，当用户进行历史对话时会找到对应用户对应uid的对话，拿出并拼接到提示词中。  

在与大模型对话时，我们会提供RAG功能，将产品的使用说明按照条数进行切片（我们预设了一些问题和回答，一组算一条），然后获得相似度比较高的一些结果，将他拼到提示词模板中。

我们会给大模型一些定义好的工具，比如说查询天气功能（调用API实现），在用户描述位置时，大模型发送对应的调用工具json，我们会将调用结果拼接到提示词中发送给大模型。等待大模型没有调用请求后获得最终对话结果，进行结果的返回。

此外，我们对多种大模型进行了工厂式的封装，支持大模型的根据模型名称注册返回

**AI更改版本**

我这个项目是一个面向产品售后场景的 AI 智能问答服务平台。项目目标是让用户在产品使用、故障排查、常见售后咨询这些场景下，能够通过多轮对话获得比较准确的回答，同时支持知识增强、工具调用和多会话管理。

整个服务端是基于 C++ 自研 HTTP 服务框架实现的，底层网络通信依赖 Muduo，上层封装了 HTTP 请求解析、路由分发、中间件、Session 管理、数据库连接池等模块。我主要负责的是后端主链路设计和实现，包括登录态管理、聊天主流程、多模型接入、消息异步持久化以及工具调用这一块。

在用户接入上，项目支持注册和登录。用户登录成功后，服务端会通过 Session 机制维护登录态，也就是通过 Cookie 中的 sessionId 识别当前用户。除此之外，项目还额外维护了一个聊天会话 ID，用来区分同一个用户的不同对话窗口。这样用户只需要登录一次，但可以同时拥有多个独立的聊天上下文，不会发生上下文串扰。

在聊天主链路上，用户发起请求后，服务端会根据当前用户 ID 和聊天会话 ID 找到对应的会话上下文，并把历史消息组织起来传给模型。为了保证历史对话访问速度，项目会在启动时从数据库恢复聊天记录到内存结构里，按用户和会话维度管理，后续查询历史消息时优先从内存读取。

在模型接入这部分，我采用了 Strategy + Factory 的方式做统一封装。也就是说，不同模型的 API 地址、请求格式和响应解析逻辑都被抽象成不同的 AIStrategy 实现类，业务主流程不直接依赖具体模型细节，只通过工厂根据 modelType 创建对应策略对象。这样后续如果要增加新的云模型或者本地模型，不需要改主流程，只要新增一个策略类并注册到工厂里就行。

在智能能力上，这个项目除了普通多轮对话，还支持两类增强能力。第一类是检索增强问答，也就是在售后场景里把产品说明、常见问答这类知识作为额外信息源，在用户提问时将相关内容作为上下文增强模型回答。第二类是工具调用，比如天气查询这种外部能力。这里的实现方式是：先通过 prompt 把可用工具及参数格式告诉模型，如果模型判断需要调用工具，就返回一个 JSON，服务端解析后执行对应工具函数，再把工具结果拼接进新的提示词中，发起第二次模型请求，生成最终回答。当前这部分实现的是轻量级单轮工具调用，也就是支持一次工具请求和一次结果回填。

在工程实现上，我还做了一个比较重要的优化，就是把聊天消息的持久化改成了 RabbitMQ 异步写库。因为如果每次用户发消息和模型回消息都同步写 MySQL，会阻塞主请求线程，影响响应速度。所以现在的做法是：主链路先把消息写入内存上下文，并立即返回模型结果；与此同时，把 SQL 写入任务投递到 RabbitMQ，由后台消费者线程异步消费并写入数据库。这样做的主要目的是把对话响应和持久化解耦，提升整体吞吐能力。

## AI遇到的问题

1. 多会话聊天上下文容易混淆

遇到的问题：最开始如果只按用户维度维护一个聊天上下文，那么同一个用户在多个对话窗口之间切换时，历史消息会混在一起，导致模型上下文串扰。

比如用户先在一个窗口问售后问题，再新建另一个窗口问天气或其他问题，如果系统只按 userId 维护一份 AIHelper/messages，那第二个窗口的请求会带上第一个窗口的历史，最终导致回答不准确。

我是怎么解决的：我把会话管理从“单用户单上下文”改成了：userId -> sessionId -> AIHelper也就是按 用户 ID + 聊天会话 ID 双层管理上下文。

每个聊天窗口独立维护一个 AIHelper 和对应历史消息，同时再额外维护 sessionsIdsMap 来保存用户拥有的会话列表。这样就实现了单用户多会话隔离，解决了上下文串扰问题。

2. 聊天主链路同步写数据库会阻塞响应

遇到的问题：如果用户每发一条消息、模型每回一条消息都同步写 MySQL，那么数据库写入延迟会直接阻塞当前请求，导致聊天响应时间变长。

尤其是在多轮对话场景下，一次聊天要连续记录用户消息和 AI 回复，如果数据库写入放在主线程里，随着并发增加，接口延迟会明显上升。

我是怎么解决的：我把消息持久化从同步写库改成了 RabbitMQ 异步入库：主链路先把消息写入当前会话内存，然后把 SQL 任务投递到 RabbitMQ 队列。后台消费者线程再异步消费并写入 MySQL

这样做之后，聊天响应链路和持久化链路被解耦，主流程只关注“尽快返回模型结果”，数据库压力被转移到了异步消费者侧。

3. 不同模型接口格式不统一，主流程容易耦合

遇到的问题：不同厂商模型的 API 地址、请求格式、响应格式都不一样。如果把这些逻辑都直接写在聊天主流程里，代码会变得非常臃肿，而且后续新增模型时修改成本很高。

当项目同时支持普通聊天模型、RAG 模型和工具调用模型时，这个问题会特别明显。因为它们不仅 URL 不一样，甚至请求 JSON 结构也不同。

我是怎么解决的：我用 Strategy + Factory 做了一层抽象：AIStrategy 统一定义模型接入接口。各模型策略分别实现 buildRequest、parseResponse、getApiUrl 等方法。StrategyFactory 根据 modelType 创建具体策略对象

这样 AIHelper 就不需要关心底层细节，只负责调度，新增模型时也只需要增加新的策略类并注册进工厂，降低了主流程耦合。

4. 工具调用结果和模型回答之间需要二次衔接

遇到的问题：在工具调用场景下，模型不能直接得到最终答案，因为它先要决定是否调用工具；而工具返回的是结构化数据，不能直接原样返回给用户，还需要模型基于工具结果重新组织自然语言答案。

在什么情况下遇到：比如天气查询这类问题，用户说“明天北京天气怎么样”，模型需要先返回一个工具调用 JSON，服务端执行天气 API 后，再让模型基于天气结果生成最终自然语言回复。

我是怎么解决的：我把工具调用设计成一个两阶段流程：  

第一轮模型判断是否调用工具，并返回 JSON，服务端解析 JSON，执行对应工具，将工具结果拼接到新的提示词中。  
第二次请求模型，生成最终回答  
这样工具调用和自然语言生成被串联起来，既能利用外部真实数据，又能保持回复形式自然。


5. 历史对话恢复和内存上下文一致性问题

遇到的问题：聊天上下文主要维护在内存里，如果服务重启，内存里的历史消息就会丢失，导致用户之前的会话无法继续。

在什么情况下遇到：用户登录后希望继续查看和使用历史对话，但如果服务端内存已经重置，仅靠当前请求本身拿不到完整上下文。

我是怎么解决的：我在服务启动时，通过 readDataFromMySQL() 从 chat_message 表中读取历史聊天记录，并按 userId + sessionId 维度恢复到内存中的 AIHelper 结构里。
这样用户重新登录后，就可以基于数据库中恢复的历史消息继续对话，保证了会话连续性。